---
title: Sub 1.4 / Phase 2 planning hooks (consolidated from Sub 1.2)
author: head-1.2 (planning-hooks subagent)
subphase: "1.2"
date: 2026-05-06
affected_tracks: [alpha-m, gamma, delta, combined, infrastructure]
urgency: important
status: ready for PlannerAI consumption
---

# Sub 1.4 / Phase 2 planning hooks (from Sub 1.2)

PlannerAI: this is the consolidated forward-looking digest from Sub 1.2.
Read this BEFORE assembling Sub 1.3 / 1.4 / Phase 2 plans. All section
references resolve into the Sub 1.2 source docs indexed in §8.

---

## §1 Sub 1.2 outcomes affecting Sub 1.4 (per-protein recipe map)

Default Sub 1.4 mapping (informed by Sub 1.2 generalization evidence; see
`1.2-mace-npt-fixed.md` §Generalization status + `1.2-so3lr-rescue-results.md`):

| Protein | Atoms (solv) | Sub 1.4 default | Justification |
|---------|------|-----------------|---------------|
| WW | 7,565 | MACE-OFF24 NPT (R3 recipe LOCKED) | 100 ps clean; SO3LR vacuum needs float64+dt=0.25fs+chain=5 numerical rescue |
| GB3 | 9,874 | MACE-OFF24 NPT (R3 recipe LOCKED) | 25 ps probe clean; SO3LR neutral-protonation gate PASSED 500 ps 2026-05-02 |
| GB1 | (small) | MACE-OFF24 NPT — verify with 50 ps probe (diagnostic-first) before 5+ ns | Recipe expected to generalize; not yet recipe-validated |
| NTL9 | (~390 protein-atoms; ≤10K solv) | MACE-OFF24 NPT — verify with 50 ps probe before 5+ ns | Recipe expected to generalize; SO3LR neutral-prot gate PASSED 500 ps |
| UBQ | 17,259 | **D-UBQ-1 contingent**: NVT-only (option b) **OR** ubq_alt 1XQQ NPT (option d) **OR** NTL9 substitute (option c) | NaN @ 7-9.6 ps across dt={1.0, 0.5, 0.25} fs; pattern asymptotic = architectural at ~17K-atom scale from 1UBQ crystal |

**Cross-cutting protocol commits:**
- All net-charged proteins (|Q|>0; D/E or K/R unbalanced) → mandatory neutral-protonation re-prep (LYN/ASH/GLH variants) for SO3LR vacuum runs. Locked in OSF v3 §4.
- WW SO3LR vacuum → float64 + dt=0.25 fs + NHC chain=5 + thermo=200 dt-units (numerical, not chemical, rescue).
- Buffer-sr/lr at SO3LR defaults 1.25/1.25 (audit-revert; tighter values risk silent NL underflow at t=0).

---

## §2 Round 3 + Round 4 recipe locks (DO NOT PROPOSE CHANGES)

**MACE-OFF24 NPT Round 3 (locked; OSF v3 §4 deposit-bound):**
sentinel-bond + protein HBonds + dt=1 fs + MonteCarloBarostat freq=25 always-on + LangevinMiddleIntegrator (300 K, 1 ps⁻¹) + OpenCL platform (CUDA broken on H200).

**MACE NPT Round 4 small wins (applied 2026-05-05 to `mace_hybrid_npt_prod.py`):** `getState` reuse refactor (defensive API; no current consumer) + progress JSON cadence drop to per-checkpoint + KE/TE drop from `StateDataReporter`. Forward-looking only (in-flight R3 jobs unaffected).

**SO3LR NVT Round 4 (applied 2026-05-05 to `so3lr_rescue.sbatch`):** XLA `--xla_gpu_enable_async_collectives=true` + `XLA_PYTHON_CLIENT_PREALLOCATE=false` + `CUDA_MODULE_LOADING=LAZY`.

**BioEmu Rounds 1-4 locks:** length-aware `batch_size_100`, GPU keepalive, MSA precache, disorder pre-screen, oversampling formula `num_samples = ceil(2000 / pass_rate * 1.3)`, RTX 5000 Ada lock (never H200/B200), NPZ post-success cleanup, 3-tier walltime classification (short L<200 → 6h Standard `gpu`; medium L=200-499 → 12h scavenge_gpu; long L≥500 → 24h scavenge_gpu), `--cpus-per-task=4`, `--mem=24G` for L<200 cohort.

---

## §3 Compute-cost vs quality directive (PlannerAI elevation candidate)

**Per user directive 2026-05-05 (recorded as `completion-report.md` §13 #10).** Every SLURM job exercises BOTH levers — cheapest viable partition AND most efficient code/parameters — never just one. Quality guardrails LOCKED: trajectory length, force-field choice, statistical thresholds, OSF v3 pre-registration, benchmark roster.

PlannerAI: elevate to `shared/notes/operational-practices.md` § Compute-cost vs quality directive as durable cross-subphase guidance. Reference in every Sub 1.4+ task spec preamble.

---

## §4 Storage tier policy (PlannerAI elevation candidate)

**Per user directive 2026-05-04 (recorded as `completion-report.md` §13 #9).** Scratch = large/non-permanent (logs, intermediate arrays, caches). Home = code/docs/manifests/<10 MB ref data. Project = permanent shared (envs, large reference DBs). Motivated by 2026-05-04 disk survey: `pi_mg269` project tier at 95% bytes / 76% files (binding constraint).

PlannerAI: elevate to `shared/notes/operational-practices.md` § Storage tier policy. Apply in Sub 1.4+ SLURM wrappers (default `--output=/--error=` to scratch; `CONDA_PKGS_DIRS` / `HF_HOME` / `JAX_COMPILATION_CACHE_DIR` / `XDG_CACHE_HOME` under scratch).

---

## §5 Risk register (R6/R7/R8) — elevation candidates

Per `1.2-closure-master-plan.md` §Annotations 2026-05-03:

- **R6: scavenge_gpu preemption integrity.** Trajectory stitching across SLURM REQUEUE not yet audited. Pre-Sub-1.4 audit needed: verify that checkpoint-restart yields contiguous + numerically reproducible trajectories under preemption (no drift; correct frame stride; HBonds/sentinel state preserved across resume).
- **R7: per-protein recipe-fallback compute multiplier (Sub 1.4).** Budget ~3-5× wall for proteins requiring NVT-only or Tier-2 NPT (slower dt; gentler ramp; larger padding). Affects MACE production budgeting if multiple proteins follow UBQ pattern.
- **R8: `prio_su_enforce.sh` failure-mode docs.** False negatives → SU overrun (silent budget cap exceeded). False positives → mid-equil scancel (e.g., WW gate cancellation 2026-05-02 at 22:58Z). Document operator semantics + recovery.

PlannerAI: elevate to `shared/notes/operational-practices.md` as durable cross-subphase guidance.

---

## §6 Sub 1.3 unblocking actions (Gamma central ablation)

- **RMSF: COMPLETE.** 47 per-protein `*_rmsf.csv` + 46 `*_rmsf.npz` + aggregate `batch1_rmsf.csv` + summary at `output/features/`. Compute script at `output/scripts/features/compute_rmsf_batch1.py`.
- **RSALOR: PARTIAL — RSA done, LOR/RSALOR stubbed.** Delivered 2026-05-06: 47 per-protein RSA CSVs + NPZs + aggregate (10,010 residue rows) at `output/features/`. RSA columns populated (`rsa_absolute_a2`, `rsa_relative` via Shrake-Rupley + Tien 2013 max ASA). LOR/RSALOR NaN-stubbed. **FULL mode unblocking actions for Sub 1.3:** (i) fetch ProteinGym MSAs from `https://github.com/OATML-Markslab/ProteinGym` `DMS_substitutions` bundle; (ii) `pip install rsalor` into env-bioemu (clone to env-bioemu-rsalor; never modify env-bioemu in place); (iii) re-run `compute_rsalor_batch1.py --msa-dir <path>` (script handles per-protein MSA-missing gracefully).
- **Trajectory analysis skeletons: SHIPPED 2026-05-06.** 7 files at `output/scripts/analysis/` (1236 LOC): `analyze_mace_npt.py`, `analyze_so3lr_vacuum.py`, `S2_compute.py`, `density_T_P.py` (FULLY IMPLEMENTED), `contacts.py`, `__init__.py`, `README.md`. NotImplementedError stubs labeled `Sub 1.4 SubAgent: <specific guidance>` for trajectory loading + S2 autocorrelation. Argparse CLIs + JSON/NPZ writers + density_T_P + S2 orchestration + Rg/COM/NaN-onset are complete.

---

## §7 Open user decisions PlannerAI should know about

- **D-UBQ-1**: probe verdicts pending (10622876 NTL9 substitute + 10622885 UBQ_alt 1XQQ both PENDING on scavenge_gpu). Decision tree in `shared/help-needed/head-1.2-mace-ubq-non-generalization.md` §Decision rules. Both PASS → keep UBQ + NTL9 substitute optional; UBQ_alt PASS / NTL9 FAIL → 1XQQ becomes UBQ representative; NTL9 PASS / UBQ_alt FAIL → criterion #1 substitution; both FAIL → option (b) NVT pivot for UBQ.
- **2026-05-09 contingency**: scavenge_gpu dispatch monitor. If no scavenge dispatch by then (LevelFS=0.27 still depressed), surface option to move MACE production to Standard `gpu_h200` (10× SU; requires user approval per 3× partition rule).
- **OSF deposit hard 2026-05-15** (preferred 2026-05-13 / backup 2026-05-14). User deposits manually; head-1.2 records DOI in `1.2-osf-deposited.md` stub.
- **Working-tree push pending user authorization.** Uncommitted since commit 3a79078 (2026-05-04): R4 script edits, 3 BioEmu sbatch wrappers, 3 round-4 reports, doc batch updates 3+4, completion-report §13 #9 + #10 directives, RSALOR + analysis-skeleton outputs (2026-05-06).

---

## §8 Cross-references (Sub 1.2 cross-agent notes index)

| File | One-liner |
|------|-----------|
| `1.2-bioemu-batch2-passrates.md` | Batch 2 actual vs predicted pass rates; 2026-05-05 reorg appendix |
| `1.2-bioemu-precache-keepalive-fix.md` | MSA precache + GPU keepalive infra fix |
| `1.2-closure-master-plan.md` | Strategy backbone; R1-R8 risk register; day-by-day timeline; D-UBQ-1 + D-OSF-SO3LR decision points |
| `1.2-delta-baselines-results.md` | 5/5 Delta baselines + WMSE harness on 100K Tahoe-100M; D3 retired |
| `1.2-env-so3lr-typing-extensions-fix.md` | env-so3lr typing-extensions hotfix |
| `1.2-gpu-util-efficiency.md` | GPU utilization profiling across MACE/SO3LR/BioEmu |
| `1.2-mace-conda-path-fix.md` | Conda path canonicalization (verbatim path required) |
| `1.2-mace-npt-fixed.md` | **CRITICAL** — Round 3 recipe locked; per-protein generalization status; UBQ caveat |
| `1.2-mace-npt-prod-launch.md` | Production driver design + probe outcomes incl. UBQ option-d |
| `1.2-mace-npt-stability.md` | Stability evidence summary (preliminary); R4 row appended |
| `1.2-mace-throughput-ceiling.md` | Throughput optimization investigation (CUDA/cuequivariance/GPU NL/f32) |
| `1.2-osf-deposited.md` | OSF DOI + deposit metadata STUB (promote post-deposit) |
| `1.2-scope-recommendations.md` | Forward-looking scope items from post-Sub-1.1 audit |
| `1.2-so3lr-pilot-stability.md` | v1 pilot outcomes 2/5 PASS pre-rescue |
| `1.2-so3lr-rescue-results.md` | **CRITICAL** — chemistry rescue gate verdicts; 5 ns submissions; production-protocol commit; R4 row |
| `1.2-stats-pipeline-validation.md` | 5/5 stats pipeline components pass synthetic-data unit tests |

---

*End of Sub 1.4 / Phase 2 planning hooks. PlannerAI: read in conjunction with `phases/phase-1/subphase-1.2/completion-report.md` §13 (recommendations 1-10) for full context.*
