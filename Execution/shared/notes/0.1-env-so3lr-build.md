---
subphase: "0.1"
topic: "env-so3lr-build"
tracks_affected: [alpha-m]
urgency: info
created: 2026-04-15T19:00:00Z
---

# env-so3lr Build Result: SUCCESS

## Status

**env-so3lr built successfully.** SO3LR 0.1.0 installed with JAX 0.5.3 and jax-md 0.2.27.

## Details

- Python: 3.12 (required by SO3LR >=3.12)
- JAX: 0.5.3 (with CUDA 12 support)
- jax-md: 0.2.27
- SO3LR: 0.1.0 (installed from GitHub)
- Smoke test: `import jax; import jax_md; import so3lr` — PASSED
- Warnings: TensorFlow not installed (expected, not needed)

## Impact on D1 Gate (May 9)

**Positive signal for D1.** env-so3lr is ready for Phase 1 MLFF pilot studies.
The SO3LR software dependency is satisfied. Both MACE and SO3LR environments built
successfully, suggesting D1 GO is likely.

## Notes

- SO3LR requires Python >=3.12 (not 3.10 like other envs). This is a separate env.
- JAX pinned to 0.5.3 for SO3LR compatibility. Do not upgrade without testing.
- GPU verification will happen in Phase 1 when actual simulations run.
- YAML exported to `envs/env-so3lr.yml`.
